[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "query-adapters"
version = "0.1.0"
description = "Query adapters for data warehouses"

[tool.poetry.dependencies]
python = "^3.8"
